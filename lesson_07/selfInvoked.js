(function () {
    const number = parseFloat((Math.random() * (10 - 1) + 1).toFixed(1));
    console.log(number);
    return number;
})();


(function () {
    const number = parseFloat((Math.random() * (100 - 1) + 1).toFixed(1));
    console.log(number);
    return number;
})();